const cart = [
	{name: "Widget", price: 9.95},
	{name: "Gadget", price: 22.95}
];

//Gives the Names in the Array
//const names = cart.map(x => x.name);
//console.log(names);

//Gives the Prices in the Array
//const prices = cart.map(x => x.price);
//console.log(prices);

//Uses Prices in a Function to Find out the Prices After a 20% Aale 
//const disPrices = prices.map(x => x*0.8);
//console.log(disPrices);

//Uses Names in the Pre-Built Function .toLowerCase to Give the Names in Lower Case
//const lcNames = names.map (name => name.toLowerCase());
//console.log(lcNames);